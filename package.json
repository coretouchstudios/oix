{
  "name": "oix",
  "private": true,
  "packageManager": "npm@10.8.1",

  "workspaces": [
    "apps/*",
    "packages/*"
  ],

  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build --filter=web-platform",
    "lint": "turbo run lint"
  },

  "devDependencies": {
    "turbo": "^2.8.0",
    "typescript": "^5.4.0"
  }
}